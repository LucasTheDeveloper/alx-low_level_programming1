#include<stdio.h>
/**
 * main - we are using puts function to print a line instead of a printf
 * Return:0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
